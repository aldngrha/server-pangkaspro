<%- include("../../partials/header")%>
<%- include("../../partials/navbar") %>
<%- include("../../partials/sidebar") %>

<div class="p-4 sm:ml-64">
    <div class="p-4 mt-14">
        <div class="flex flex-col gap-y-2 pb-10 md:-mt-16">
            <h1 class="text-2xl font-medium">All Kapster</h1>
            <p class="text-md md:text-lg font-light text-gray-400">Kelola membermu!</p>
        </div>
        <div class="bg-white w-full rounded-lg">
            <div class="p-6">
                <%- include("../../partials/alert") %>
                <div class="relative overflow-x-auto shadow-md sm:rounded-lg">
                    <table class="w-full text-sm text-left text-gray-500 dark:text-gray-400">
                        <div class="flex flex-col md:flex-row md:items-center md:justify-between">
                            <h1 class="md:p-5 mb-2 text-lg font-semibold text-left text-gray-900 bg-white">All User List</h1>
                            <a href="/user/create" class="text-sm mb-2 font-normal text-white px-2 py-2 rounded-md bg-secondary hover:bg-secondary-hover hover:text-gray-100"><i class="bx bx-plus mr-1"></i>Tambah</a>
                        </div>
                        <thead class="text-xs text-gray-700 uppercase bg-gray-50 dark:bg-gray-700 dark:text-gray-400">
                        <tr>
                            <th scope="col" class="px-6 py-3">
                                Nama Pengguna
                            </th>
                            <th scope="col" class="px-6 py-3">
                                Sebagai
                            </th>
                            <th scope="col" class="px-6 py-3">
                                Action
                                <span class="sr-only">Action</span>
                            </th>
                        </tr>
                        </thead>
                        <tbody>
                        <% for(let i = 0; i < users.length; i++) {%>
                            <tr class="bg-white border-b dark:bg-gray-800 dark:border-gray-700">
                                <th scope="row" class="px-6 py-4 font-medium text-gray-900 whitespace-nowrap dark:text-white">
                                    <%= users[i].name %>
                                </th>
                                <td class="px-6 py-4">
                                    <%= users[i].role %>
                                </td>
                                <td class="px-6 py-4 flex">
                                    <a href="/user/edit/<%= users[i]._id %>" class="p-2 mr-1 bg-yellow-300 rounded-md text-white hover:bg-yellow-500"><i class="bx bxs-pencil"></i></a>
                                </td>
                            </tr>
                        <%}%>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
<%- include("../../partials/footer")%>